self.addEventListener("install", () => {
  self.skipWaiting();
});

self.addEventListener("activate", (event) => {
  event.waitUntil(self.clients.claim());
});

// Pass-through; presence enables installability. No offline caching.
self.addEventListener("fetch", () => {});
