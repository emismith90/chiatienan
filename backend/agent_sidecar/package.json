{
  "name": "kernos-sidecar",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "description": "kernos sidecar: owns the Pi harness for any kernos host. Python keeps the money.",
  "engines": {
    "node": ">=22.19"
  },
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.84.1",
    "@earendil-works/pi-coding-agent": "^0.84.1",
    "typebox": "^1.3.13"
  }
}
