# Phase 11 — the room's own CMS: view, edit, revision log, republish

Written 2026-09-06 on `claude/headless-cms-pi-harness-nn18pb`, after Phase 10.

**The ask.** A link in the room that shows the CMS of the agent that room runs. Anyone in
the room can view and edit it. Keep the revision log so a change can be traced. Add a way
to republish an existing version.

Unlike Phases 5–10, **this ships on**. It is a new door into the content plane, opened to
seven people who are not admins, on a bot that moves real money. The design work is
almost entirely about what that door does *not* open.

---

## 1. Facts from the code

1. **Server-side, most of this already exists.** What is missing is a room-scoped door.
   - `kn_profile_versions` **is** the revision log: `version`, `status`
     (`draft|published|superseded|retired`), the whole `spec`, `actor`, `note`,
     `created_at`, `published_at`. Read with `store.list_versions(profile_id)`.
   - `kn_audit_log` (`actor`, `action`, `entity`, `entity_id`, `before`, `after`, `at`)
     is written by `store.log(...)`.
   - **`store.rollback(profile_id, version, *, actor, gates, override_reason)` already
     republishes a superseded version**, skipping gates 3 and 4 because that version
     passed its probe and its eval when it was first published. Exposed today only at
     `POST /api/admin/profiles/{id}/rollback`.
   - Editing is `create_draft` → `update_draft(patch)` → `publish(gates=…)`.
2. **Two unrelated auth systems.** Room routes take `Depends(require_session)` → a bearer
   token → `AuthCtx(member_id, room_id, display_name, nickname)`, then `_check_room(ctx,
   room_id)`. The admin router takes `X-Admin-Password`, a shared secret, with a
   self-declared `X-Actor` header. **There is no admin identity** (design §9). A room
   member, by contrast, *is* identified — which is what makes an honest revision log
   possible here for the first time.
3. **Two vocabularies already govern who may change what.**
   - `gates.BLACKLIST_FIELDS` = `builtin_tools, models, tool_packs, pipeline, eval,
     extensions, settings, runtime, caps`. Enforced against **agent** actors (gate 5);
     a human publish may currently touch all of it.
   - `capabilities.SCOPE_VOCABULARY` = `prompt.body, prompt.append, skills, rules,
     validation.warn`.
   - A rule tagged `money` is refused to agents (`cms_draft_change`); a human may edit it.
   - Gate 2: a money profile with a risky builtin tool needs an `override_reason` on
     every publish — the lunch profile always does.
4. **Sources are upstream of drafts** (the Phase 8 lesson). `create_draft(snapshot=True)`
   pulls the business's current `kn_sources` into the draft. So a change written only to
   a version's `spec` is silently reverted by the next snapshotting draft.
   `approve_proposal` already handles this correctly via `_apply_source_changes`.
5. **The frontend has the pattern.** `side-panel.tsx` is a tab strip (`ledger`,
   `knowledge`) with both panels mounted and the inactive one hidden; the fetch is gated
   on the active tab so a room that never opens it never pays for it.
   `place-dialog.tsx` / `note-dialog.tsx` are the dialog pattern.
6. **The lunch profile is the business default**, shared by every space with no binding of
   its own. Editing it changes the bot in every such room.

## 2. Decisions

**D1 — "Edit" is scoped; "view" is not.** A room member may read the whole resolved
profile, including the parts they cannot change. They may write only
`prompt.body`, `prompt.append`, `skills` and `rules` — the `SCOPE_VOCABULARY` minus
`validation.warn` — and never a rule tagged `money`. Everything in `BLACKLIST_FIELDS` is
refused for a `member:` actor exactly as it is for an `agent:` actor.

*This is a deliberate narrowing of the literal ask and the user must confirm it.* Taken
literally, "anyone in the room can edit" lets any of seven people swap `models` (spends
money at a different rate, and gate 3 would demand a probe), raise `caps`, change
`pipeline`, or delete the money-safety rule that stops the bot doing arithmetic in prose.
The admin-password path keeps all of that. The room path does not. One constant
(`ROOM_EDITABLE`) widens it if the user disagrees.

**D2 — The actor is `member:<id>`.** Same form the Phase 10.3 proposal card already uses
for a confirm. This is what makes the revision log worth keeping: every version names a
real person, not `admin`.

**D3 — Optimistic concurrency on the published version.** The client sends the
`base_version_id` it loaded. If the profile's `published_version_id` has moved since,
the write is refused with 409 and the current version, rather than silently overwriting
another member's edit.

**D4 — An edit writes the source *and* the version.** Otherwise fact 4 bites: the next
snapshotting draft pulls the old source back and the room's change vanishes with no error
anywhere. Reuse the path `approve_proposal` uses.

**D5 — Republish is `store.rollback`, unchanged.** It already refuses anything that is not
`superseded`, already skips gates 3/4 with a recorded reason, and already writes a new
version rather than mutating history — so republishing v2 over v5 leaves v5 in the log.
The room route passes `actor="member:<id>"` and an `override_reason` naming the member,
because gate 2 demands one for this profile.

**D6 — The room edits the shared profile, and the UI says so.** The alternative —
copy-on-write into a room-specific profile on first edit — is safer in principle but
changes what the room is running, stops it tracking boot updates (`managed_by` leaves
`"boot"`), and needs a space binding. With one real room the practical risk is nil and
the honesty is cheap: the panel states, above the editor, that this bot is shared by
every room that has not been given its own. Recorded as the known limit, with
copy-on-write as the follow-up when a second real room exists. This is consistent with
the warning the Phase 10.3 proposal card already carries.

**D7 — No model-visible change.** This phase adds routes and UI. The prompt, the tool
manifest and the pipeline are untouched, so the benchmark does not need re-running for
the *phase* — only for whatever a member subsequently edits, which is their own doing and
what the revision log and republish button exist for.

## 3. Tasks

### 11.1 — the read API and the link

`app/main.py`, all `Depends(require_session)` + `_check_room`:

- `GET /api/rooms/{room_id}/agent` → `{agent: {slug, name, persona}, profile: {id, name,
  business}, version: {id, version, actor, note, published_at}, editable: {prompt_body,
  prompt_append, skills:[{name, description, body}], rules:[{slug, content, tags,
  editable}]}, readonly: {models, caps, builtin_tools, tool_packs, pipeline_stages},
  shared: bool, scope: [...]}`. `rules[].editable` is false for a money-tagged rule.
- `GET /api/rooms/{room_id}/agent/versions` → the revision log: `version`, `status`,
  `actor`, `note`, `created_at`, `published_at`, and `paths` (what changed against the
  previous version, from `gates.changed_paths`).
- `GET /api/rooms/{room_id}/agent/versions/{version}` → that version's editable content
  plus a unified diff against its predecessor (reuse `osadmin._unified`).

**Proof:** a member of the room reads it; a member of *another* room gets 403; no bearer
token gets 401; the money rule comes back `editable: false`; `shared` is true for the
seeded lunch profile.

### 11.2 — edit and publish

`PUT /api/rooms/{room_id}/agent/content`, body `{base_version_id, note, prompt_body?,
prompt_append?, skills?, rules?}`:

1. Refuse if `base_version_id` ≠ the profile's current `published_version_id` (409).
2. Build the patch from the supplied fields only. Refuse any blacklisted path and any
   money-tagged rule with a message naming the field (400).
3. `create_draft(from published, snapshot=False)` → `update_draft(patch,
   actor=f"member:{id}")` → write the changed sources → `publish(gates=…,
   actor=f"member:{id}", override_reason=f"room edit by member:{id}", note=note)`.
4. Return the new version and the paths it changed.

**Proof:** a prompt edit publishes and the next turn's system prompt contains it; a skill
edit is still there after a later snapshotting draft (D4); `models` in the body is
refused; a money rule is refused; a stale `base_version_id` is refused with the current
version; the audit log and `kn_profile_versions` both name `member:<id>`; a gate failure
(e.g. an empty prompt body if gate 1 refuses it) leaves the published version untouched.

### 11.3 — republish

`POST /api/rooms/{room_id}/agent/versions/{version}/republish`, body `{note?}` →
`store.rollback(profile_id, version, actor=f"member:{id}", gates=…,
override_reason=f"republished from the room by member:{id}")`.

**Proof:** republishing a superseded version makes it the published spec again as a *new*
version number; the version it replaced becomes `superseded` and is still in the log; a
`draft`/`retired`/currently-published version is refused with the reason; the room's next
turn uses the republished content.

### 11.4 — the frontend

- `side-panel.tsx`: a third tab, `Agent`. Fetch gated on the active tab, like `knowledge`.
- `agent-panel.tsx`: the shared-bot notice; the editable fields (prompt body, append
  lines, skills, rules) with money rules shown locked and labelled; the read-only block
  (model, caps, packs) shown but not editable; the revision list with actor, when, note
  and changed paths; a version opens its diff; a **Republish** button on any superseded
  version, with a confirm.
- `lib/api.ts`: the four calls. `use-room.ts`: bump a version counter on a successful
  write so the panel and the chat both refetch.
- Tests in `src/components/chat/__tests__/agent-panel.test.tsx`.

**Proof:** the panel renders from a fixture; the money rule has no editable control;
saving calls the API with the loaded `base_version_id`; a 409 shows a "someone else
changed it, reload" message rather than retrying; Republish asks first.

### 11.5 — docs and verification

README (the room CMS, what a member may and may not change, and why); the deploy runbook
(this one ships **on** — the first thing in these phases that does); the plan's state of
play; `TODO.md` for copy-on-write per room. Full backend suite, frontend tests, golden
9/9, layering, the 19-tool manifest, and the unedited-pre-existing-tests check.

## 4. Open questions for the review gate

1. **D1** — is a scoped room editor right, or should "anyone can edit" mean literally
   everything, with the gates as the only guard?
2. **D6** — shared profile plus a warning, or copy-on-write into a room-owned profile on
   first edit?
3. Is there a path by which a room member's edit can defeat the money invariants —
   through `prompt.body` (the whole system prompt is in scope), a skill body, or a
   non-money rule that contradicts `money-safety`?
4. **D4** — is writing sources on a room edit correct, or does it have a failure mode the
   proposal path does not (it changes the business's future drafts, and the business is
   shared with the poker profile's room)?
5. Does republish need to be narrower than `rollback` (which skips gates 3 and 4) when the
   actor is a room member rather than an operator handling an incident?

---

## 5. Review gate (second Fable, 2026-09-06) — findings and dispositions

> Sections 1–4 above are the record of what was *proposed*. Where they differ from
> §6 below, **§6 wins**. Two blockers; both verified against the code by hand before
> being accepted.

| id | sev | finding | disposition |
|----|-----|---------|-------------|
| F1 | blocker | `POST /api/rooms/create` is unauthenticated ("public by design", multi-room spec). A stranger creates a room, gets a bearer token, and that room — having no `kn_space_bindings` row — resolves to the lunch business's **default agent**, i.e. the same profile the seven real users run. `_check_room` passes for their own room, so 11.2 as written lets anyone on the internet republish the real bot's prompt and skills. | **Taken, and it reshapes the phase.** Verified: `app/main.py:340 create_room_public` takes no auth. Editing is gated on an explicit binding, not on membership of any room that happens to resolve to the profile — see D0. Unbound rooms are view-only. |
| F2 | blocker | The money rule can be overwritten by slug collision. A submitted rule `{slug: "money-safety", tags: []}` is a *non*-money rule to `gates._money`, so it passes the tag check; the D4 source write then `put_source(..., "rule", "money-safety", frontmatter={"tags": []})`, and `kn_sources` being unique on `(business, kind, slug)` this replaces the money rule's body **and strips its tag**. The next snapshotting draft pulls the untagged rewrite in and the money rule is gone from every future version. | **Taken.** The tag is not an identity — the slug is. Guard on slug, in the shared `_source_changes` so the proposal path is covered too. See D1 as amended. |
| F3 | major | Fact 1, D5 and 11.3 misdescribe `store.rollback`. It does **not** write a new version: it calls `publish()` on the *same row*, so v2 becomes published again under number 2. And `publish` sets `v.published_at = utcnow()` and overwrites `v.note` when a note is passed — which `rollback` always does. History **is** mutated, and the plan's `{note?}` can never reach the store. | **Taken; I had told the user "nothing is rewritten", which was wrong.** Verified at `store.py:341-380`. Republish becomes `create_draft(from_version=N, snapshot=False)` → `publish`, which makes the "new version" claim true and gives the member's note somewhere to live. |
| F4 | major | Republish lets a member revert operator-only fields. `rollback` only checks `status == "superseded"`, so a member can republish a version whose `models`, `caps`, `builtin_tools`, `pipeline` or money rules differ from the current one — and `skip_probe=True` puts a possibly unprobed model live. | **Taken.** The room route refuses a target where `outside_scope(current, target, ROOM_EDITABLE)` is non-empty. With that, the gate 3/4 skip is moot because the blacklisted fields are identical by construction. |
| F5 | major | Republish forgets D4: it moves the version but leaves the sources at the newer content, so the next snapshotting draft silently re-applies the change the member just undid. Fact 4's lesson, one task later. | **Taken.** Republish writes sources through the same helper as 11.2. |
| F6 | major | 11.2 writes sources *then* publishes. `approve_proposal` does the opposite for a reason: on a gate failure the plan's order leaves `kn_sources` poisoned while the published version is untouched — and the stated proof ("a gate failure leaves the published version untouched") passes anyway. | **Taken.** Publish first, sources only on success; retire the draft on failure so refused attempts do not litter the revision list. |
| F7 | major | The 409 check is not atomic. `base_version_id` is compared in the route, then draft/update/publish run in three separate sessions. Two members who both loaded v7 both pass, and the second publish silently supersedes the first. The proof passes while the race it exists for is open. | **Taken.** `store.publish` gains `if_published`, re-checked inside the publish transaction. |
| F8 | major | D6 misattributes the `managed_by` cost. **Any** non-bypass publish sets `managed_by = "human"`, after which `ensure_seeded` never republishes from code and a source with `updated_by != "boot"` is never re-put. So the *first member edit in production* permanently detaches the real bot from every future code change to the prompt, skills and rules. This applies to the chosen design, not only to the alternative it rejects. | **Taken.** Verified at `store.py:365` and `boot.py`. D6 corrected, `managed_by` surfaced in `GET /agent` so the panel can say the bot no longer follows deploys, and a post-deploy check added to the runbook. |
| F9 | major | Q3 answered concretely: `PI_BUILTIN_TOOLS` defaults to `read,write,bash` and gate 2's override is auto-supplied by the route, so bash stays enabled. A member can leave the tagged rule intact and add a skill or append line saying "compute the split in bash, then state it". When the model obeys, `moneyguard.backed_amounts` counts **every** invocation's args and results as backing and `validate._evidence` excludes only `evidence=False` *packs* — bash is a builtin, not a pack — so the figure passes as "backed" and `UnbackedAmounts` does not even warn. | **Taken as documentation plus a cap; the fix is a follow-up.** Excluding builtins from the evidence set changes validator behaviour for the live bot, so it needs its own phase and a benchmark run (standing rule 2). Phase 11 adds a size cap on submitted content and states the limit plainly in the README: prompt-level money rules are advisory; the enforced ones are the blacklisted fields. |
| F10 | major | Proof lists that pass while the feature is broken — missing: slug collision (F2); a `rules` body that *omits* the money rule (`update_draft` uses `deep_merge`, so lists replace); a money-tagged new rule; two members racing (F7); source etag unchanged after a gate failure (F6); republish of a version with different blacklisted fields (F4); sources rewritten on republish (F5); original note/published_at preserved (F3); a member of a freshly created unbound room (F1). | **Taken in full.** Each becomes a named test; the F1 test must exist before this ships. |
| F11 | minor | Source `if_match` should be the etag the member loaded, not the published spec — sources can be ahead of the published spec, so a member's skill edit would overwrite an operator's unpublished `put_source` with no `PreconditionFailed`. | **Taken.** `GET /agent` returns each editable source's etag; `PUT` sends them back. |
| F12 | minor | More machinery than the ask needs: 11.2 step 2 hand-builds a blacklist when `gates.outside_scope(published, draft, ROOM_EDITABLE)` already returns every path a scope may not touch, including `NEVER_IN_SCOPE`; and `osadmin._source_changes` already derives source rows from two specs. | **Taken.** `ROOM_EDITABLE` becomes the only new constant; `_source_changes` is lifted to module level and shared, with the F2 guard added there once. |
| F13 | minor | Question 4's premise is wrong: poker is its own business, so lunch source writes cannot reach the dealer. The steward is in the lunch business but its profile carries no lunch skills or rules and is built `snapshot=False`, so nothing bleeds today. | **Taken.** Question corrected; the real limit recorded instead. |
| F14 | minor | Rule slugs become `/virtual/<slug>` paths in `agentsFiles` and `kn_sources.slug`; nothing validates them. | **Taken.** `^[a-z0-9][a-z0-9._-]{0,79}$`, and `..` / `/` refused. |
| F15 | minor | Other members never learn the version moved until they hit a 409. | **Taken.** Publish and republish emit an `agent:changed` room event, like `ledger:changed`. |
| F16 | nit | `list_versions` returns `spec: None`, so per-version `changed_paths` is N extra reads; and `readonly.pipeline_stages` would expose plugin `config`, which can carry prose. | **Taken.** Cap the list at 50 and render stage names only. |

## 6. Decisions and tasks as amended

**D0 (new, from F1) — editing requires an explicit binding.** A room may edit the profile
it runs only if `kn_space_bindings` has a row for that space. An unbound room — including
every room a stranger can create — gets the read API and the panel in **view-only** mode,
with the shared-bot notice explaining why. Shipping therefore includes one operator step:
bind the real room to the `phoenix` agent (`PUT /api/admin/spaces/{room}/binding`). That
binding resolves to the same agent and the same profile, so it is not a behaviour change;
it is an authorisation fact. This replaces "membership of the room" as the permission.

**D1 as amended** — scope is enforced as `outside_scope(published, draft, ROOM_EDITABLE)
== []` with `ROOM_EDITABLE = ("prompt.body", "prompt.append", "skills", "rules")`, not a
hand-rolled field list (F12). On top of that, **slug is identity** (F2): a submitted rule
whose slug matches a money-tagged rule in the published spec is refused, duplicate slugs
and skill names within one submission are refused, and the set of money-tagged rules after
the edit must be byte-identical to the published set. The gate's reasoning for keeping
`prompt.body` in scope stands: the `money-safety` rule duplicates the prompt's money block,
so removing one leaves the other, and the residual risk is the same across body, append,
skills and rules — F2 and F9 are the real exposure, not the field list.

**D3 as amended** — two checks, not one: `base_version_id` at the top of the route for a
fast, friendly 409, and `store.publish(..., if_published=base_version_id)` re-checked
inside the publish transaction so the race actually closes (F7). Source writes carry the
etags the member loaded (F11).

**D4 as amended** — **publish first, sources on success** (F6), with the draft retired on
gate failure. Republish writes sources too (F5). The shared `_source_changes` helper
carries the F2 slug guard so the proposal path inherits it.

**D5 as amended** — `store.rollback` is *not* what republish uses. It re-publishes the same
row and destroys that version's original `note` and `published_at` (F3). Republish is
`create_draft(from_version=N, snapshot=False)` → `publish(...)`, producing a genuinely new
version that carries the member's note and leaves the whole history intact. The target is
refused if `outside_scope(current, target, ROOM_EDITABLE)` is non-empty (F4).

**D6 as amended** — the room edits the shared profile, and editing is binding-gated (D0).
Copy-on-write is **not** the safer alternative it looked like: `kn_sources` is per
*business*, so a room-owned profile would still share skills and rules and would not
isolate anything without per-profile sources. Recorded in `TODO.md` with that caveat.

**D8 (new, from F8) — the first member edit detaches the bot from deploys.** Any non-boot
publish flips `managed_by` to `"human"`, after which `ensure_seeded` stops republishing
from code and boot stops re-putting sources. This is the main operational consequence of
shipping this on. `GET /agent` returns `managed_by` so the panel can say so, and the
runbook gains a post-deploy check.

**D9 (new, from F9) — the money invariants that survive are the enforced ones.** Refused:
`pipeline` (both validators), `tool_packs`, `builtin_tools`, blocking `validation`,
`meta.handles_money`, `rules[tag=money]`. Not enforced: prose. With `bash` enabled, an
in-scope text edit can instruct the model to compute in bash, and `backed_amounts` counts
bash output as evidence, so `UnbackedAmounts` will not even warn. Phase 11 caps submitted
content size (32 KB total) and says this plainly in the README. The fix — excluding
builtins from the evidence set — is a follow-up phase with its own benchmark run.

**Tasks as amended.** 11.1 gains `managed_by`, source etags, `can_edit`, a 50-version cap
and stage-names-only. 11.2 becomes: check binding → check `base_version_id` → build draft
→ `outside_scope` + slug guards + size cap → publish with `if_published` → sources on
success → emit `agent:changed`; draft retired on failure. 11.3 becomes create-draft-from-
version → scope check → publish → sources → event. 11.4 renders view-only for an unbound
room and shows `managed_by`. 11.5 adds the binding step and the `managed_by` check to the
runbook. Every proof in F10 is a named test, and the unbound-stranger test gates the ship.

---

## 7. Self-validation against the gate (2026-09-06, after implementation)

Each finding, the code that answers it, and the named test that would fail if the answer
were removed. Run them with
`.venv/bin/python -m pytest tests/test_room_cms.py tests/test_os_admin.py tests/test_proposals.py`.

| id | answered in | proved by | state |
|----|-------------|-----------|-------|
| F1 | `roomcms._editable_space` — 403 unless `describe()` reports `bound` | `test_an_unbound_room_may_look_but_never_edit` (asserts the unbound room resolves to the *real* profile, then that both writes 403), `test_binding_the_room_opens_editing_and_changes_nothing_else` | **closed** |
| F2 | `kernos/content/sources.py` — `money_slugs`/`protected_changes` keyed on the **published** slug; `osadmin.cms_draft_change` uses the same guard | `test_a_money_rule_cannot_be_taken_over_by_reusing_its_slug`, `test_dropping_the_money_rule_from_the_list_is_refused`, `test_a_member_cannot_mint_a_new_money_rule_or_repeat_a_slug` | **closed** |
| F3 | `roomcms.republish` drafts `from_version` and publishes that, instead of `store.rollback` | `test_republish_makes_a_new_version_and_leaves_the_whole_history_intact` — asserts the earlier rows keep their own `note` and `published_at` | **closed** |
| F4 | `roomcms.republish` runs `protected_changes` + `outside_scope` on the target | `test_republish_refuses_a_version_that_changes_what_a_member_may_not` | **closed** |
| F5 | `roomcms.republish` calls `apply_source_changes` after publishing | `test_republish_restores_the_sources_too` — including that a later snapshotting draft keeps it | **closed** |
| F6 | `roomcms.edit` publishes first, applies sources on success, retires the draft in `except BaseException` | `test_a_gate_failure_leaves_every_source_and_the_published_version_untouched` | **closed** |
| F7 | `store.publish(if_published=…)`, re-checked inside the publish transaction | `test_publish_rechecks_inside_its_own_transaction` (the store-level race) and `test_two_members_who_both_loaded_the_same_version_cannot_both_win` (the route) | **closed** |
| F8 | `view()` returns `profile.managed_by`; the panel renders the warning | `test_the_first_member_edit_detaches_the_bot_from_deploys_and_says_so`; `agent-panel.test.tsx` "warns when the bot is shared, and when it has stopped following deploys" | **closed (documented, not prevented — it is inherent)** |
| F9 | `MAX_CONTENT_BYTES` cap; the limit written down in `roomcms`'s docstring, D9 and the README | `test_the_content_a_room_can_make_every_turn_carry_is_capped` | **partially open, deliberately.** The cap ships; excluding builtins from `backed_amounts` changes live validator behaviour and is a follow-up phase with its own benchmark. |
| F10 | every listed proof exists | the 20 tests in `tests/test_room_cms.py` | **closed** |
| F11 | `view()` returns `source_etags`; `edit` compares them before publishing | `test_a_source_edited_since_the_member_loaded_it_is_refused` | **closed** |
| F12 | `outside_scope(published, spec, ROOM_EDITABLE)` rather than a hand-rolled list; `source_changes` lifted to `kernos.content.sources` and shared | `test_scope_is_the_published_vocabulary`, `test_the_blacklisted_fields_are_not_reachable_from_a_room` (4 params) | **closed** |
| F13 | question corrected in §5 | — (documentation) | **closed** |
| F14 | `SLUG_RE` in `_validate_names` | `test_rule_slugs_and_skill_names_are_validated` (5 params, incl. `../escape`) | **closed** |
| F15 | `agent:changed` published by both routes; `use-room.ts` bumps `agentVersion` | `agent-panel.test.tsx` refetches on `version` change | **closed** |
| F16 | `VERSION_LIMIT = 50`; `readonly.pipeline_stages` is stage names | `test_the_read_view_separates_what_may_change_from_what_may_not`, `test_the_revision_log_names_the_member_and_what_they_changed` | **closed** |

**Two answers I owe the gate directly.** Q1: the scope is `ROOM_EDITABLE` enforced through
`outside_scope`, exactly as recommended. Q2: option (a) — the binding — is what shipped;
copy-on-write is not in `TODO.md` as "safer" but as "would need per-profile sources to
mean anything", which is what the gate established.

**What is *not* closed, stated plainly:** F9's second half. A member with edit rights can
still write prose that talks the model into doing arithmetic in `bash`, and
`moneyguard.backed_amounts` will count that output as evidence, so no validator warns.
The hard invariants (a ledger write needs a confirmed card; `FabricatedCommit` blocks a
forged "Đã ghi") are untouched. The follow-up is a phase of its own.
