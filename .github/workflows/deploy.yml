name: Deploy

# Continuous deployment to the DigitalOcean droplet.
#
# Model: images are BUILT ON GITHUB and pushed to GHCR; the droplet only
# PULLS them (docker compose pull && up -d). No `--build` on the droplet, so
# the 512 MB host never runs `next build` and cannot OOM during a deploy.
#
# Runs on merges to main and on manual dispatch. The deploy job stays a no-op
# until the SSH secrets are configured, so merging never breaks the repo.
# Configure under Settings → Secrets and variables → Actions:
#
#   Secrets (required for deploy):
#     DEPLOY_SSH_KEY   – private SSH key authorized on the droplet
#     DEPLOY_HOST      – droplet hostname or IP
#   Secrets (required to (re)generate .env from GitHub; if unset the deploy
#   keeps the existing .env on the droplet untouched):
#     OPEN_ROUTER_KEY  – OpenRouter key for the agent sidecar (note the name:
#                        OPEN_ROUTER_KEY, not OPENROUTER_API_KEY)
#     ADMIN_PASSWORD   – admin password guarding POST /api/rooms etc.
#   Secrets (optional):
#     DEPLOY_USER      – SSH user (defaults to "root")
#     DEBUG_API_KEY    – guards the read-only /internal/debug/* export API
#                        (deploy/DEBUGGING.md §6). Unset ⇒ written empty ⇒ the
#                        routes stay disabled; the run warns when that happens.
#                        Generate: python -c "import secrets; print(secrets.token_urlsafe(32))"
#   Variables (all optional except CADDY_DOMAIN; each falls back to its
#   .env.example default when unset):
#     CADDY_DOMAIN             – your domain (REQUIRED when .env is regenerated; Caddy TLS)
#     DEPLOY_PATH              – checkout dir on the droplet (default /opt/chiatienan)
#     PI_MODEL                 – default "~deepseek/deepseek-v4-flash-latest"
#     PI_VISION_MODEL          – default "qwen/qwen3-vl-30b-a3b-instruct". Mandatory
#                                in practice: PI_MODEL is text-only, so every bill
#                                photo routes here.
#     PI_PROVIDER              – default "openrouter"
#     PI_THINKING              – default "medium"
#     DATA_DIR                 – default "/data". MUST stay under the mounted
#                                volume: it holds SQLite and every room's
#                                long-term memory.
#     PI_MAX_TOOLS             – default "40"
#     PI_MAX_SECONDS           – default "120"
#     (no base-URL variable: the OpenRouter URL is a constant in the sidecar)
#     BOT_HANDLE               – default "bot"
#     DATABASE_URL             – default "sqlite:////data/chiatienan.db" (keep it
#                                under /data so the SQLite file persists)
#     TZ                       – default "Asia/Ho_Chi_Minh"
#     LOG_FILE                 – default "/data/logs/app.log" (rotating mirror of
#                                the stdout log; what /internal/debug/logs serves.
#                                Keep it under /data so it survives a restart)
#     LOG_MAX_BYTES            – default "5000000"
#     LOG_BACKUP_COUNT         – default "3"
#     QR_BASE_URL              – default "https://img.vietqr.io/image"
#     QR_TEMPLATE              – default "compact2"
#
# GHCR auth uses the run's built-in GITHUB_TOKEN (packages: write to push,
# packages: read on the droplet to pull) — no personal access token needed,
# and the packages stay private.

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: false

permissions:
  contents: read

env:
  IMAGE_BASE: ghcr.io/${{ github.repository_owner }}/chiatienan

jobs:
  build:
    name: Build & push images (GHCR)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push backend
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          push: true
          tags: |
            ${{ env.IMAGE_BASE }}-backend:latest
            ${{ env.IMAGE_BASE }}-backend:${{ github.sha }}
          cache-from: type=gha,scope=backend
          cache-to: type=gha,mode=max,scope=backend

      - name: Build & push frontend
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          push: true
          tags: |
            ${{ env.IMAGE_BASE }}-frontend:latest
            ${{ env.IMAGE_BASE }}-frontend:${{ github.sha }}
          cache-from: type=gha,scope=frontend
          cache-to: type=gha,mode=max,scope=frontend

  deploy:
    name: Deploy to droplet
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    steps:
      - name: Check configuration
        id: check
        env:
          KEY: ${{ secrets.DEPLOY_SSH_KEY }}
          HOST: ${{ secrets.DEPLOY_HOST }}
          OPEN_ROUTER_KEY: ${{ secrets.OPEN_ROUTER_KEY }}
          ADMIN_PASSWORD: ${{ secrets.ADMIN_PASSWORD }}
        run: |
          if [ -z "$KEY" ] || [ -z "$HOST" ]; then
            echo "configured=false" >> "$GITHUB_OUTPUT"
            echo "::notice::DEPLOY_SSH_KEY / DEPLOY_HOST not set — skipping deploy. See deploy.yml header."
            exit 0
          fi
          echo "configured=true" >> "$GITHUB_OUTPUT"
          if [ -n "$OPEN_ROUTER_KEY" ] && [ -n "$ADMIN_PASSWORD" ]; then
            echo "provision_env=true" >> "$GITHUB_OUTPUT"
          else
            echo "provision_env=false" >> "$GITHUB_OUTPUT"
            echo "::notice::OPEN_ROUTER_KEY/ADMIN_PASSWORD not set — keeping the existing .env on the droplet."
          fi

      - name: Set up SSH
        if: steps.check.outputs.configured == 'true'
        env:
          DEPLOY_SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
        run: |
          set -euo pipefail
          mkdir -p ~/.ssh
          printf '%s\n' "$DEPLOY_SSH_KEY" > ~/.ssh/id_deploy
          chmod 600 ~/.ssh/id_deploy
          ssh-keyscan -H "$DEPLOY_HOST" >> ~/.ssh/known_hosts 2>/dev/null

      - name: Provision .env from secrets
        if: steps.check.outputs.configured == 'true' && steps.check.outputs.provision_env == 'true'
        env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_USER: ${{ secrets.DEPLOY_USER || 'root' }}
          DEPLOY_PATH: ${{ vars.DEPLOY_PATH || '/opt/chiatienan' }}
          OPEN_ROUTER_KEY: ${{ secrets.OPEN_ROUTER_KEY }}
          ADMIN_PASSWORD: ${{ secrets.ADMIN_PASSWORD }}
          DEBUG_API_KEY: ${{ secrets.DEBUG_API_KEY }}
          CADDY_DOMAIN: ${{ vars.CADDY_DOMAIN }}
          PI_MODEL: ${{ vars.PI_MODEL || '~deepseek/deepseek-v4-flash-latest' }}
          PI_VISION_MODEL: ${{ vars.PI_VISION_MODEL || 'qwen/qwen3-vl-30b-a3b-instruct' }}
          PI_PROVIDER: ${{ vars.PI_PROVIDER || 'openrouter' }}
          PI_THINKING: ${{ vars.PI_THINKING || 'medium' }}
          PI_MAX_TOOLS: ${{ vars.PI_MAX_TOOLS || '40' }}
          PI_MAX_SECONDS: ${{ vars.PI_MAX_SECONDS || '120' }}
          DATA_DIR: ${{ vars.DATA_DIR || '/data' }}
          DATA_DIR_FALLBACK: /data
          BOT_HANDLE: ${{ vars.BOT_HANDLE || 'bot' }}
          DATABASE_URL: ${{ vars.DATABASE_URL || 'sqlite:////data/chiatienan.db' }}
          # Sourced from the repo `TZ` variable; kept as APP_TZ on the runner so
          # it can't shift the runner's own clock/timestamps.
          APP_TZ: ${{ vars.TZ || 'Asia/Ho_Chi_Minh' }}
          LOG_FILE: ${{ vars.LOG_FILE || '/data/logs/app.log' }}
          LOG_MAX_BYTES: ${{ vars.LOG_MAX_BYTES || '5000000' }}
          LOG_BACKUP_COUNT: ${{ vars.LOG_BACKUP_COUNT || '3' }}
          QR_BASE_URL: ${{ vars.QR_BASE_URL || 'https://img.vietqr.io/image' }}
          QR_TEMPLATE: ${{ vars.QR_TEMPLATE || 'compact2' }}
        run: |
          set -euo pipefail
          if [ -z "${CADDY_DOMAIN}" ]; then
            echo "::error::CADDY_DOMAIN variable is required to (re)generate .env (Caddy needs it for TLS)."
            exit 1
          fi
          if [ -z "${DEBUG_API_KEY}" ]; then
            echo "::warning::DEBUG_API_KEY secret is unset — writing it empty, so the /internal/debug/* export API stays disabled (404s). Add the secret to enable it."
          fi
          # /data is the only mounted volume (docker-compose.yml: ./data:/data).
          # Anywhere else is container-local and every deploy recreates the
          # backend, so every room's long-term memory would be wiped on each ship.
          # Refuse to write a non-persistent path.
          case "${DATA_DIR}" in
            /data|/data/*)
              ;;
            *)
              echo "::warning::DATA_DIR='${DATA_DIR}' is not under the mounted /data volume, so every room's memory would be wiped on every deploy. Using '${DATA_DIR_FALLBACK}' instead — update or delete the repo variable to silence this."
              DATA_DIR="${DATA_DIR_FALLBACK}"
              ;;
          esac
          umask 077
          # Mirrors .env.example. Secrets + commonly-tuned values come from
          # GitHub; the rest are stable defaults. The unquoted heredoc expands
          # the ${VARS} once, writing their literal values.
          cat > env.generated <<EOF
          # Managed by .github/workflows/deploy.yml — edits here are overwritten on deploy.
          OPEN_ROUTER_KEY=${OPEN_ROUTER_KEY}
          PI_MODEL=${PI_MODEL}
          PI_VISION_MODEL=${PI_VISION_MODEL}
          PI_PROVIDER=${PI_PROVIDER}
          PI_THINKING=${PI_THINKING}
          PI_MAX_TOOLS=${PI_MAX_TOOLS}
          PI_MAX_SECONDS=${PI_MAX_SECONDS}
          DATA_DIR=${DATA_DIR}
          BOT_HANDLE=${BOT_HANDLE}
          DATABASE_URL=${DATABASE_URL}
          TZ=${APP_TZ}
          ADMIN_PASSWORD=${ADMIN_PASSWORD}
          DEBUG_API_KEY=${DEBUG_API_KEY}
          LOG_FILE=${LOG_FILE}
          LOG_MAX_BYTES=${LOG_MAX_BYTES}
          LOG_BACKUP_COUNT=${LOG_BACKUP_COUNT}
          QR_BASE_URL=${QR_BASE_URL}
          QR_TEMPLATE=${QR_TEMPLATE}
          CADDY_DOMAIN=${CADDY_DOMAIN}
          EOF
          # Install atomically on the droplet, backing up the previous .env.
          # The generated file is piped over ssh stdin (never on the argv).
          ssh -i ~/.ssh/id_deploy -o StrictHostKeyChecking=yes "${DEPLOY_USER}@${DEPLOY_HOST}" \
            "cd '${DEPLOY_PATH}' && { [ -f .env ] && cp -a .env \".env.bak.\$(date +%Y%m%d%H%M%S)\" || true; } && cat > .env.new && install -m 600 .env.new .env && rm -f .env.new && echo '.env provisioned from GitHub secrets'" < env.generated
          rm -f env.generated

      - name: Deploy (pull prebuilt images)
        if: steps.check.outputs.configured == 'true'
        env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_USER: ${{ secrets.DEPLOY_USER || 'root' }}
          DEPLOY_PATH: ${{ vars.DEPLOY_PATH || '/opt/chiatienan' }}
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GHCR_USER: ${{ github.actor }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          set -euo pipefail
          # Token is piped via ssh stdin into `docker login --password-stdin`,
          # so it never appears in the remote process arguments. git pull keeps
          # docker-compose.yml / Caddyfile current; IMAGE_TAG pins the exact
          # images this run built. `caddy reload` applies any Caddyfile change
          # (docker compose up -d won't recreate the unchanged caddy container,
          # so its bind-mounted config wouldn't otherwise take effect); it's a
          # graceful, zero-downtime no-op when the config is unchanged.
          #
          # Two deliberate details, both learned from a green run that shipped
          # nothing (the 512 MB/25 GB droplet had filled up):
          #   * Reclaim disk BEFORE `pull`, and with `-af` — pruning dangling
          #     images afterwards is too late and too little. Images backing a
          #     running container are never touched by prune, so the currently
          #     serving containers survive this.
          #   * Every step is `;`-separated under `set -e`, so the remote exit
          #     status is the FIRST failure. The old form ended
          #     `… || true; docker compose ps`, which made `ps` the exit status
          #     and masked "no space left on device" from `pull` as a success.
          echo "${GHCR_TOKEN}" | ssh -i ~/.ssh/id_deploy -o StrictHostKeyChecking=yes \
            "${DEPLOY_USER}@${DEPLOY_HOST}" \
            "set -e; cd '${DEPLOY_PATH}'; git pull --ff-only; \
             docker login ghcr.io -u '${GHCR_USER}' --password-stdin; \
             export IMAGE_TAG='${IMAGE_TAG}'; \
             df -h /var/lib/docker; \
             docker image prune -af; docker builder prune -af; \
             df -h /var/lib/docker; \
             docker compose pull; docker compose up -d; \
             { docker compose exec -T caddy caddy reload --config /etc/caddy/Caddyfile || echo '::warning::caddy reload failed; previous config still active'; }; \
             { docker logout ghcr.io >/dev/null 2>&1 || true; }; \
             docker compose ps --format '{{.Service}} {{.Image}}'"

      - name: Verify the running images match this commit
        if: steps.check.outputs.configured == 'true'
        env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_USER: ${{ secrets.DEPLOY_USER || 'root' }}
          DEPLOY_PATH: ${{ vars.DEPLOY_PATH || '/opt/chiatienan' }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          set -euo pipefail
          # A successful `pull` still does not prove the new containers are the
          # ones serving traffic, so assert the tag rather than trusting the
          # step above. Fails the deploy when prod is left on stale code.
          running=$(ssh -i ~/.ssh/id_deploy -o StrictHostKeyChecking=yes \
            "${DEPLOY_USER}@${DEPLOY_HOST}" \
            "cd '${DEPLOY_PATH}'; export IMAGE_TAG='${IMAGE_TAG}'; docker compose ps --format '{{.Service}} {{.Image}}'")
          printf '%s\n' "$running"
          status=0
          for svc in backend frontend; do
            img=$(printf '%s\n' "$running" | awk -v s="$svc" '$1 == s { print $2 }')
            case "$img" in
              *:"${IMAGE_TAG}") echo "$svc is running ${IMAGE_TAG} ✓" ;;
              "") echo "::error::$svc has no running container"; status=1 ;;
              *) echo "::error::$svc is running '$img' — expected tag ${IMAGE_TAG}. Prod is on stale code."; status=1 ;;
            esac
          done
          exit "$status"
